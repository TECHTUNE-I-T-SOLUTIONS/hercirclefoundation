name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          # Next.js 16 requires Node >= 20.9.0; the matrix uses 20.x so the runner will pick a compatible 20+ version
          node-version: 20.9.0

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          # optional: pin a pnpm version, or omit to install the latest
          version: 8

      - name: Install dependencies
        # Some hosted runners may have lockfile format mismatches; allow install to proceed in CI
        run: pnpm install --no-frozen-lockfile

      - name: Build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: pnpm build
